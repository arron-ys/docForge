# 墨衡演示数据管理平台 HLD

## 系统模块

当前版本明确支持用户登录、项目看板、文档上传、资料解析、文档草稿生成、DOCX 导出。

系统由上传登记模块、解析模块、证据抽取模块、模板确认模块、草稿生成模块和 DOCX 导出模块组成。

上传登记模块保存资料来源类型。解析模块读取文本资料。证据抽取模块区分参考资料和产品资料。模板确认模块保留人工确认边界。

## 截图边界

截图文件只用于登记和后续补图建议，当前版本不进行 OCR，不调用视觉模型，也不从图片内容提取产品事实。

